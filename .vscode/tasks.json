{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "build:axum-api",
      "command": "cargo",
      "args": ["build"],
      "options": {
        "cwd": "${workspaceFolder}/axum-api"
      }
    },
    {
      "type": "shell",
      "label": "build:cli-client",
      "command": "cargo",
      "args": ["build"],
      "options": {
        "cwd": "${workspaceFolder}/cli-client"
      }
    }
  ]
}
