{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build axum-api",
      "command": "cargo",
      "args": ["build"],
      "options": {
        "cwd": "${workspaceFolder}/axum-api"
      }
    }
  ]
}
